<div class='container index'>
<h1>All Players</h1>

<% @players.each do |player|%>
    <p class="player" id="<%= player.id %>">
        <%= image_tag player.image if player.image.present? %>
        <%= link_to player.name, player_path(player.id) %>
    </p>
<% end %>
</div>
